// Any - when the compiled doesn't know able to pridict the what value will inferred to the variable in feture it will set with any.
let hero;

const getHero = () => {
  return "Abhishek";
};
hero = getHero();
